<Window x:Class="OnEntryDesktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OnEntry Login" Height="350" Width="400"
        Background="#1a1a1a" WindowStartupLocation="CenterScreen">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="OnEntry" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <TextBox x:Name="EmailBox" Grid.Row="1" Height="35" Margin="0,0,0,15" Background="#2d2d2d" Foreground="White" BorderBrush="#444" Padding="8" PlaceholderText="Email"/>

        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Height="35" Margin="0,0,0,25" Background="#2d2d2d" Foreground="White" BorderBrush="#444" Padding="8"/>

        <Button Grid.Row="3" Height="40" Background="#2563eb" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Click="Login_Click">
            Sign In
        </Button>
    </Grid>
</Window>